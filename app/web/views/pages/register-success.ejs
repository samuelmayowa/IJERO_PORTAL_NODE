<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= typeof title !== 'undefined' ? title : 'Success' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <style>
    :root{ --green:#247D57; --wine:#82103C; }
    html,body{height:100%;}
    .page{position:relative; min-height:100vh;}
    .bg-cover{position:absolute; inset:0; background:url('/public/img/login-bg.jpg') center/cover no-repeat;}
    .frost{position:absolute; inset:0; background:#fff; opacity:.85;}
    .wrap{position:relative; z-index:2; max-width:760px; margin:10vh auto; background:#fff; border-radius:12px;
          box-shadow:0 12px 30px rgba(0,0,0,.10); overflow:hidden;}
    .head{background:var(--green); color:#fff; padding:18px 24px; font-size:20px; font-weight:700;}
    .body{padding:28px;}
    .big{font-size:18px;}
    .btn-wine{background:var(--wine); color:#fff; font-weight:700;}
    .btn-wine:hover{filter:brightness(.95); color:#fff;}
    .btn-green{background:var(--green); color:#fff; font-weight:700;}
    .btn-green:hover{filter:brightness(.95); color:#fff;}
  </style>
</head>
<body>
<div class="page">
  <div class="bg-cover"></div>
  <div class="frost"></div>

  <div class="wrap">
    <div class="head">
      <i class="fa <%= (typeof headingIcon !== 'undefined' ? headingIcon : 'fa-check-circle') %>"></i>
      <%= (typeof headingText !== 'undefined' ? headingText : 'Registration Successful') %>
    </div>

    <div class="body">
      <p class="big mb-3">
        <% if (typeof messageHtml === 'string' && messageHtml.length) { %>
          <%- messageHtml %>
        <% } else { %>
          Welcome <b><%= (typeof full_name !== 'undefined' && full_name) ? full_name : 'User' %></b>! Your portal account was created successfully.
        <% } %>
      </p>
      <p class="text-muted">
        You can now proceed to your dashboard to continue with payments, applications, or course-related activities.
      </p>

      <div class="d-flex mt-3">
        <a href="<%= (typeof primaryHref !== 'undefined' ? primaryHref : (typeof next !== 'undefined' ? next : '#')) %>" class="btn btn-wine mr-2">
          <i class="fa fa-arrow-right"></i> <%= (typeof primaryLabel !== 'undefined' ? primaryLabel : 'Continue to Dashboard') %>
        </a>
        <a href="<%= (typeof secondaryHref !== 'undefined' ? secondaryHref : '/') %>" class="btn btn-green">
          <i class="fa fa-home"></i> <%= (typeof secondaryLabel !== 'undefined' ? secondaryLabel : 'Homepage') %>
        </a>
      </div>
    </div>
  </div>
</div>
</body>
</html>
