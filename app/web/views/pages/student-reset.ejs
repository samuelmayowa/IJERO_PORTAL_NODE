<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title><%= typeof title === 'string' ? title : 'Reset Password' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="csrf-token" content="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

  <style>
    :root {
      --green: #247D57;
    }

    body, html {
      height: 100%;
      margin: 0;
      padding: 0;
      background: url('/public/img/login-bg.jpg') center/cover no-repeat fixed;
    }

    .overlay {
      position: absolute;
      inset: 0;
      background: rgba(255, 255, 255, 0.9);
      z-index: 0;
    }

    .page-wrapper {
      position: relative;
      z-index: 1;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .card-reset {
      width: 100%;
      max-width: 450px;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
      overflow: hidden;
    }

    .card-reset .head {
      background: var(--green);
      color: #fff;
      font-weight: bold;
      padding: 1rem;
      text-align: center;
    }

    .card-reset .body {
      padding: 1.5rem;
    }

    .form-group label {
      font-weight: 500;
    }

    @media (max-width: 576px) {
      .card-reset {
        margin: 0 auto;
      }

      .btn {
        width: 100%;
        margin-bottom: 0.5rem;
      }

      .btn-link {
        width: 100%;
        text-align: center;
      }
    }
  </style>
</head>
<body>

<div class="overlay"></div>

<div class="page-wrapper">
  <div class="card-reset">
    <div class="head">Reset Password (Student)</div>
    <div class="body">

      <% if (messages && messages.error) { %>
        <div class="alert alert-danger"><%= messages.error %></div>
      <% } %>
      <% if (messages && messages.success) { %>
        <div class="alert alert-success"><%= messages.success %></div>
      <% } %>

      <form method="post" action="/student/reset">
        <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">

        <div class="form-group">
          <label>Account Type</label>
          <select name="role" class="form-control" required>
            <option value="student">Student</option>
            <option value="applicant">Applicant</option>
          </select>
        </div>

        <div class="form-group">
          <label>Surname</label>
          <input name="surname" class="form-control" required />
        </div>

        <div class="form-group">
          <label>Date of Birth</label>
          <input name="dob" type="date" class="form-control" required />
        </div>

        <div class="form-group">
          <label>Phone</label>
          <input name="phone" class="form-control" required />
        </div>

        <div class="form-group d-flex flex-wrap align-items-center">
          <button type="submit" class="btn btn-danger mr-2 mb-2">
            <i class="fa fa-unlock"></i> Reset to “College1”
          </button>

          <a href="/login" class="btn btn-outline-secondary mb-2">
            <i class="fa fa-sign-in"></i> Back to login
          </a>

          <a href="/register" class="btn btn-link ml-auto mb-2">Create an account</a>
        </div>
      </form>

    </div>
  </div>
</div>

</body>
</html>
